//given an object containg a title and query of a type of film, return the result of the query as an array of film cards in film row.

/**
 * 
 * @param {title} string
 * @param {query} string
 * @param {displayObj} object
 * @param {displayObj.cardType} string
 * @param {displayObj.rowType} string 
 * @return {filmRow} containing the film cards
 */
export default function CardController({title,query,displayObj}) {





}